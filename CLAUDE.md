# FaceLogin — Claude Code 入口

本仓库的唯一 agent 守则是 [`AGENTS.md`](AGENTS.md)。开始任何任务前先完整读取并遵守它，再按 [`DEVELOPMENT.md`](DEVELOPMENT.md) 的任务路由只打开相关契约或模块文档。

本文件不复制项目规则，避免 `CLAUDE.md` 与 `AGENTS.md` 漂移。模型已准备完成；是否运行模型脚本、如何构建和验证，均以 `AGENTS.md` 及其链接文档为准。
